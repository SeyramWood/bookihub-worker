// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/SeyramWood/ent/parcel"
	"github.com/SeyramWood/ent/parcelimage"
)

// ParcelImageCreate is the builder for creating a ParcelImage entity.
type ParcelImageCreate struct {
	config
	mutation *ParcelImageMutation
	hooks    []Hook
}

// SetCreatedAt sets the "created_at" field.
func (pic *ParcelImageCreate) SetCreatedAt(t time.Time) *ParcelImageCreate {
	pic.mutation.SetCreatedAt(t)
	return pic
}

// SetNillableCreatedAt sets the "created_at" field if the given value is not nil.
func (pic *ParcelImageCreate) SetNillableCreatedAt(t *time.Time) *ParcelImageCreate {
	if t != nil {
		pic.SetCreatedAt(*t)
	}
	return pic
}

// SetUpdatedAt sets the "updated_at" field.
func (pic *ParcelImageCreate) SetUpdatedAt(t time.Time) *ParcelImageCreate {
	pic.mutation.SetUpdatedAt(t)
	return pic
}

// SetNillableUpdatedAt sets the "updated_at" field if the given value is not nil.
func (pic *ParcelImageCreate) SetNillableUpdatedAt(t *time.Time) *ParcelImageCreate {
	if t != nil {
		pic.SetUpdatedAt(*t)
	}
	return pic
}

// SetImage sets the "image" field.
func (pic *ParcelImageCreate) SetImage(s string) *ParcelImageCreate {
	pic.mutation.SetImage(s)
	return pic
}

// SetNillableImage sets the "image" field if the given value is not nil.
func (pic *ParcelImageCreate) SetNillableImage(s *string) *ParcelImageCreate {
	if s != nil {
		pic.SetImage(*s)
	}
	return pic
}

// SetKind sets the "kind" field.
func (pic *ParcelImageCreate) SetKind(pa parcelimage.Kind) *ParcelImageCreate {
	pic.mutation.SetKind(pa)
	return pic
}

// SetNillableKind sets the "kind" field if the given value is not nil.
func (pic *ParcelImageCreate) SetNillableKind(pa *parcelimage.Kind) *ParcelImageCreate {
	if pa != nil {
		pic.SetKind(*pa)
	}
	return pic
}

// SetParcelID sets the "parcel" edge to the Parcel entity by ID.
func (pic *ParcelImageCreate) SetParcelID(id int) *ParcelImageCreate {
	pic.mutation.SetParcelID(id)
	return pic
}

// SetNillableParcelID sets the "parcel" edge to the Parcel entity by ID if the given value is not nil.
func (pic *ParcelImageCreate) SetNillableParcelID(id *int) *ParcelImageCreate {
	if id != nil {
		pic = pic.SetParcelID(*id)
	}
	return pic
}

// SetParcel sets the "parcel" edge to the Parcel entity.
func (pic *ParcelImageCreate) SetParcel(p *Parcel) *ParcelImageCreate {
	return pic.SetParcelID(p.ID)
}

// Mutation returns the ParcelImageMutation object of the builder.
func (pic *ParcelImageCreate) Mutation() *ParcelImageMutation {
	return pic.mutation
}

// Save creates the ParcelImage in the database.
func (pic *ParcelImageCreate) Save(ctx context.Context) (*ParcelImage, error) {
	pic.defaults()
	return withHooks(ctx, pic.sqlSave, pic.mutation, pic.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (pic *ParcelImageCreate) SaveX(ctx context.Context) *ParcelImage {
	v, err := pic.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pic *ParcelImageCreate) Exec(ctx context.Context) error {
	_, err := pic.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pic *ParcelImageCreate) ExecX(ctx context.Context) {
	if err := pic.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (pic *ParcelImageCreate) defaults() {
	if _, ok := pic.mutation.CreatedAt(); !ok {
		v := parcelimage.DefaultCreatedAt()
		pic.mutation.SetCreatedAt(v)
	}
	if _, ok := pic.mutation.UpdatedAt(); !ok {
		v := parcelimage.DefaultUpdatedAt()
		pic.mutation.SetUpdatedAt(v)
	}
	if _, ok := pic.mutation.Kind(); !ok {
		v := parcelimage.DefaultKind
		pic.mutation.SetKind(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pic *ParcelImageCreate) check() error {
	if _, ok := pic.mutation.CreatedAt(); !ok {
		return &ValidationError{Name: "created_at", err: errors.New(`ent: missing required field "ParcelImage.created_at"`)}
	}
	if _, ok := pic.mutation.UpdatedAt(); !ok {
		return &ValidationError{Name: "updated_at", err: errors.New(`ent: missing required field "ParcelImage.updated_at"`)}
	}
	if _, ok := pic.mutation.Kind(); !ok {
		return &ValidationError{Name: "kind", err: errors.New(`ent: missing required field "ParcelImage.kind"`)}
	}
	if v, ok := pic.mutation.Kind(); ok {
		if err := parcelimage.KindValidator(v); err != nil {
			return &ValidationError{Name: "kind", err: fmt.Errorf(`ent: validator failed for field "ParcelImage.kind": %w`, err)}
		}
	}
	return nil
}

func (pic *ParcelImageCreate) sqlSave(ctx context.Context) (*ParcelImage, error) {
	if err := pic.check(); err != nil {
		return nil, err
	}
	_node, _spec := pic.createSpec()
	if err := sqlgraph.CreateNode(ctx, pic.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	pic.mutation.id = &_node.ID
	pic.mutation.done = true
	return _node, nil
}

func (pic *ParcelImageCreate) createSpec() (*ParcelImage, *sqlgraph.CreateSpec) {
	var (
		_node = &ParcelImage{config: pic.config}
		_spec = sqlgraph.NewCreateSpec(parcelimage.Table, sqlgraph.NewFieldSpec(parcelimage.FieldID, field.TypeInt))
	)
	if value, ok := pic.mutation.CreatedAt(); ok {
		_spec.SetField(parcelimage.FieldCreatedAt, field.TypeTime, value)
		_node.CreatedAt = value
	}
	if value, ok := pic.mutation.UpdatedAt(); ok {
		_spec.SetField(parcelimage.FieldUpdatedAt, field.TypeTime, value)
		_node.UpdatedAt = value
	}
	if value, ok := pic.mutation.Image(); ok {
		_spec.SetField(parcelimage.FieldImage, field.TypeString, value)
		_node.Image = value
	}
	if value, ok := pic.mutation.Kind(); ok {
		_spec.SetField(parcelimage.FieldKind, field.TypeEnum, value)
		_node.Kind = value
	}
	if nodes := pic.mutation.ParcelIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   parcelimage.ParcelTable,
			Columns: []string{parcelimage.ParcelColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(parcel.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.parcel_images = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// ParcelImageCreateBulk is the builder for creating many ParcelImage entities in bulk.
type ParcelImageCreateBulk struct {
	config
	err      error
	builders []*ParcelImageCreate
}

// Save creates the ParcelImage entities in the database.
func (picb *ParcelImageCreateBulk) Save(ctx context.Context) ([]*ParcelImage, error) {
	if picb.err != nil {
		return nil, picb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(picb.builders))
	nodes := make([]*ParcelImage, len(picb.builders))
	mutators := make([]Mutator, len(picb.builders))
	for i := range picb.builders {
		func(i int, root context.Context) {
			builder := picb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ParcelImageMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, picb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, picb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, picb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (picb *ParcelImageCreateBulk) SaveX(ctx context.Context) []*ParcelImage {
	v, err := picb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (picb *ParcelImageCreateBulk) Exec(ctx context.Context) error {
	_, err := picb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (picb *ParcelImageCreateBulk) ExecX(ctx context.Context) {
	if err := picb.Exec(ctx); err != nil {
		panic(err)
	}
}
